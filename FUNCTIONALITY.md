# קטלוג פונקציונליות — EVulnTasker

**EVulnTasker** (Elizarov Vulnrabilities Tasking Manager Platform) היא מערכת RBVM (ניהול פגיעויות מבוסס סיכון) לשרת Linux. היא קולטת מודיעין CVE, מחלצת ומעשירה אותו, מתאימה אותו לנכסי הארגון, ופותחת משימות לבעלי נכסים ולצוות ציד איומים.

מסמך זה מתאר את מודולי המערכת: מה כל פונקציונליות עושה, למה היא נדרשת, ובאילו ספריות Python היא ממומשת.

---

## 1. ליבת האפליקציה והרצה

### הרצת השירות והדפסת כתובות מקומיות

**תיאור:** מפעיל את האפליקציה מתוך venv (`python3 app.py`), טוען את חבילת `app/`, ומדפיס כתובות מקומיות (localhost ו-IP ברשת).

**למה זה נדרש:** כדי להריץ סביבת בדיקה או ייצור בפקודה אחת ולדעת איך לגשת לממשק.

**ספריות Python:** ספריית התקן (`importlib`, `socket`, `os`, `sys`, `pathlib`); **Uvicorn** כשרת ASGI.

### שרת Web / API

**תיאור:** מגיש REST תחת `/api` ודפי HTML (דשבורד, מקורות, מאגרים, הגדרות, מעקב CVE, פרטי פגיעות). תהליך אחד מריץ HTTP, worker של ה-pipeline ו-scheduler.

**למה זה נדרש:** זהו ממשק העבודה של אנליסטים ומפעילי SOC.

**ספריות Python:** **FastAPI**, **Starlette**, **Uvicorn**, **Jinja2**, **python-multipart**, **orjson**.

### הגדרות סביבה

**תיאור:** טוען קונפיגורציה מ-`.env` / משתני סביבה (פורט, מסד נתונים, מפתחות NVD / Jira / Exchange / AI וכו').

**למה זה נדרש:** בלי זה אי אפשר להתאים את המערכת לסביבת הארגון בלי לשנות קוד.

**ספריות Python:** **Pydantic**, **pydantic-settings**.

### לוגים

**תיאור:** כותב לוגים ל-stderr ולקבצים מסתובבים, כולל לוג מובנה.

**למה זה נדרש:** חקירת תקלות, ביקורת, ומעקב אחרי כשלים ב-pipeline.

**ספריות Python:** ספריית התקן `logging`; **structlog**.

### התקנה, systemd והסרה

**תיאור:** סקריפטי `setup.sh` / `uninstall.sh` / `package_offline.sh`, יחידת systemd בשם `evulntasker`, התקנה ברירת מחדל ב-`/opt/evulntasker`, גיבוי DB בהסרה.

**למה זה נדרש:** פריסה יציבה על RHEL בלי תלות ב-IDE.

**ספריות Python:** Bash + systemd; האפליקציה עצמה רצה ב-Python כמפורט למעלה.

---

## 2. מסד הנתונים

### אחסון כל נתוני המערכת

**תיאור:** שומר CVEs, מקורות, נכסים, כרטיסים, ארטיפקטי זיהוי, לוג ביקורת והיסטוריית ריצות. ברירת מחדל: SQLite (`data/evulntasker.db`). בייצור: PostgreSQL.

**למה זה נדרש:** המערכת חייבת מקור אמת אחד לכל מחזור החיים של CVE.

**ספריות Python:** **SQLAlchemy 2**, **Alembic**, **psycopg2-binary**, **greenlet**; SQLite דרך מנהל התקן מובנה.

### בחירת DB בהגדרות (SQLite / PostgreSQL מקומי / PostgreSQL חיצוני)

**תיאור:** בטאב Database אפשר לבחור SQLite, PostgreSQL על אותו שרת Linux (`127.0.0.1` או Unix socket), או שרת מרוחק. שומר `VULNINTEL_DATABASE_URL` ב-`.env`, בודק חיבור, מחליף engine חי ויוצר טבלאות. נתונים קיימים ב-SQLite אינם מועתקים אוטומטית.

**למה זה נדרש:** סביבת ניסוי יכולה לרוץ על קובץ מקומי; ייצור דורש PostgreSQL ארגוני.

**ספריות Python:** **SQLAlchemy**, **psycopg2-binary**, **Pydantic**.

### שדרוג סכימה אוטומטי

**תיאור:** ב-startup יוצר טבלאות חסרות ומוסיף עמודות חדשות (למשל `vulnerabilities.status`, `audit_logs`) לקובץ SQLite קיים, כולל מילוי סטטוס מ-`pipeline_status`.

**למה זה נדרש:** `create_all` לא מוסיף עמודות לטבלה שכבר קיימת; בלי זה הדשבורד נופל אחרי שדרוג מודל.

**ספריות Python:** **SQLAlchemy** (`inspect`, `ALTER TABLE`).

---

## 3. צינור העיבוד (Pipeline) — חמישה שלבים

### תזמור הצינור (Orchestrator)

**תיאור:** לכל אירוע קליטה מריץ את חמשת השלבים לפי הסדר, מדלג אם ה-CVE כבר במסד, מעדכן סטטוס, ורושם ביקורת. בכשל לא ניתן לשחזור מסמן `FAILED` ושומר traceback.

**למה זה נדרש:** זה הלב התפעולי — בלי תזמור אין זרימה אחידה ממודיעין ועד פעולה.

**ספריות Python:** **SQLAlchemy**; **asyncio** לתור העבודה.

### שלב 1 — קליטה (Ingest)

**תיאור:** מקבל נתונים מ-Webhooks, Exchange, SMB, תיקייה מקומית ו-API, שומר אירוע קליטה, ויוצר רשומת Vulnerability אם ה-CVE חדש.

**למה זה נדרש:** מודיעין מגיע מערוצים שונים; צריך נקודת כניסה אחת.

**ספריות Python:** **FastAPI** (webhooks), **smbprotocol**, **exchangelib**, **httpx**, **SQLAlchemy**.

### שלב 2 — חילוץ (Extract)

**תיאור:** מחלץ מזהה CVE ב-regex, וגם vendor / product / version מטקסט מסומן, CSV, JSON או CPE. אם החילוץ חסר — AI fallback.

**למה זה נדרש:** קבצים ומיילים לא מגיעים בפורמט אחיד; בלי חילוץ אי אפשר להמשיך.

**ספריות Python:** ספריית התקן `re`, `csv`, `json`; **httpx** ל-AI.

### שלב 3 — העשרה (Enrich)

**תיאור:** מושך NVD 2.0 (CVSS, CWE, וקטור תקיפה, תיאור) ו-EPSS (סבירות ניצול). אם חסרים שדות — AI ממלא פערים, בלי להמציא ציון CVSS.

**למה זה נדרש:** כדי לדרג סיכון (חומרה + סבירות ניצול) ולא רק לדעת שיש CVE.

**ספריות Python:** **httpx**, **tenacity** (ניסיונות חוזרים ל-NVD/EPSS).

### שלב 4 — התאמה לנכסים (Match)

**תיאור:** מחפש את המוצר/יצרן ב-CMDB, Sonatype ובמלאי מקומי. אם אין התאמה — AI מציע בעלים/צוות.

**למה זה נדרש:** פגיעות בלי בעלים לא מטופלת. זה החיבור בין מודיעין לנכסי הארגון.

**ספריות Python:** **httpx**, **SQLAlchemy**.

### שלב 5 — פעולה (Act)

**תיאור:** פותח משימות Jira (לבעלים ולציד איומים), מייצר חוקי Sigma ושאילתות SIEM (KQL, XQL ועוד), שולח מייל ב-Exchange, ומעדכן סטטוס ל-ACTIONED. אם אין מפתחות — מצב dry-run.

**למה זה נדרש:** המערכת צריכה לא רק לדווח אלא גם להניע טיפול וציד.

**ספריות Python:** **httpx** (Jira), **exchangelib**, תבניות טקסט מובנות ל-Sigma/SIEM.

### AI Fallback ממוקד

**תיאור:** נקרא רק כשהמסלול הדטרמיניסטי נכשל או חסר (שלבים 2–4). מחזיר JSON מובנה. נרשם בביקורת כ-`AI_FALLBACK` עם reasoning/JSON בשדה `details`.

**למה זה נדרש:** מודיעין לא מובנה (מייל חופשי, קובץ חסר שדות) עדיין צריך להיכנס לצינור, בלי להמציא נתונים קריטיים.

**ספריות Python:** **httpx** מול API תואם OpenAI (`/chat/completions`).

---

## 4. מקורות מודיעין

### Webhook כללי

**תיאור:** נקודת HTTP עם טוקן ייעודי; גוף JSON/טקסט נכנס לצינור.

**למה זה נדרש:** מערכות חיצוניות (SOAR, סורקים) צריכות לדחוף CVE בזמן אמת.

**ספריות Python:** **FastAPI**, `secrets`.

### SMB / CIFS (עם משתמש AD/LDAP)

**תיאור:** מתחבר לשיתוף קבצים ארגוני, קורא קבצי טקסט (txt/csv/json/md), מחלץ CVE ו-vendor/product/version.

**למה זה נדרש:** ארגונים רבים מפיצים עדכוני ספקים בתיקיית רשת ולא ב-API.

**ספריות Python:** **smbprotocol**.

### תיקייה מקומית בשרת

**תיאור:** סורק נתיב על ה-Linux המקומי באותו אופן פרסור כמו SMB.

**למה זה נדרש:** בדיקות ידניות והטמעה בלי תלות ברשת.

**ספריות Python:** ספריית התקן `pathlib`.

### Outlook / Exchange מקומי

**תיאור:** מאזין לתיבת דואר on-prem, סורק מיילים לא נקראים ל-CVE ולמטא-דאטה, וגם שולח התראות לבעלים.

**למה זה נדרש:** התרעות ספקים מגיעות לעיתים רק בדוא"ל.

**ספריות Python:** **exchangelib**.

### Web API / פידים מתוזמנים

**תיאור:** מושך מכתובת HTTP במרווח קבוע (כולל מעטפת NVD 2.0), עם Bearer token אופציונלי.

**למה זה נדרש:** מקורות מודיעין פנימיים/חיצוניים שעובדים ב-pull.

**ספריות Python:** **httpx**, **APScheduler**.

### דילוג על CVE קיים

**תיאור:** אם ה-CVE כבר במסד — לא מריצים שוב העשרה/התאמה/פעולה.

**למה זה נדרש:** למנוע כפילויות בכרטיסים ובספאם לצוותים.

**ספריות Python:** **SQLAlchemy**.

---

## 5. ממשק משתמש (UI)

### דשבורד / תור CVE

**תיאור:** טבלת תור חיה עם חומרה, עדיפות, יצרן/מוצר, CVSS/EPSS, Jira, ומקור. מציג **PipelineStatus** כתג צבעוני (`INGESTED` עד `FAILED`), עם סינון ורענון.

**למה זה נדרש:** תמונת מצב תפעולית ל-SOC: מה בתהליך, מה נכשל, מה הסתיים.

**ספריות Python:** **Jinja2**, JavaScript בדפדפן מול **FastAPI**.

### מעקב CVE (CVE Tracker)

**תיאור:** פריט תפריט רגיל. טבלה: מספר CVE, סוג פגיעות, vendor, product, version. לחיצה על שורה פותחת גרף תחנות אופקי מתחת לשורה.

**למה זה נדרש:** לראות במהירות אילו CVE נקלטו ואיפה כל אחד עומד במסלול.

**ספריות Python:** **Jinja2** + JS; הנתונים מ-**SQLAlchemy**.

### מסך פרטי פגיעות + ציר ביקורת

**תיאור:** מציג העשרה, נכסים מותאמים, כרטיסי Jira, חבילת ציד, ואת כל רשומות `AuditLog` לפי זמן. רשומת `AI_FALLBACK` מודגשת ומוצג ה-JSON/הנימוק.

**למה זה נדרש:** שקיפות מלאה: למה ה-AI התערב, איפה נכשל הצינור, ומה בוצע.

**ספריות Python:** **FastAPI**, **Jinja2**.

### הגדרות (Settings)

**תיאור:** טאבים: Database, SMB, Local, Outlook, Web API. שמירה, בדיקת חיבור וסנכרון מיידי.

**למה זה נדרש:** חיבור המערכת לסביבה הארגונית בלי לערוך קבצי קונפיג ידנית.

**ספריות Python:** **FastAPI**, **Pydantic**, **SQLAlchemy**, **smbprotocol**, **exchangelib**, **httpx**.

### מיתוג וניווט

**תיאור:** כותרת EVulnTasker v1.0 Beta, תג AI-Powered RBVM, אייקון `EVulnTasker-ICON.png`, תפריט צד עם כפתור המבורגר.

**למה זה נדרש:** זיהוי המוצר וניווט נוח במסכים.

**ספריות Python:** HTML/CSS/JS סטטיים מוגשים דרך **Starlette** / **FastAPI**.

---

## 6. אינטגרציות חיצוניות

| מודול | תיאור | למה נדרש | ספריות Python |
|--------|--------|-----------|----------------|
| **NVD 2.0** | שליפת רשומת CVE רשמית (CVSS, CWE, CPE) | מקור האמת לתיאור ולציון חומרה | **httpx**, **tenacity** |
| **EPSS (FIRST)** | ציון סבירות ניצול בפועל | מיון לפי סיכון אמיתי ולא רק CVSS | **httpx**, **tenacity** |
| **Jira** | פתיחת משימות לבעלים ולציד איומים | העברת אחריות לצוותים | **httpx** |
| **CMDB** | חיפוש נכסים לפי vendor/product | קישור הפגיעות לנכס ולבעלים | **httpx** |
| **Sonatype** | התאמה לאפליקציות/רכיבים | בעלות אמיתית על רכיב תוכנה | **httpx** |
| **Exchange** | קריאה ושליחת דוא"ל | קליטת מודיעין והתראה לבעלים | **exchangelib** |
| **SMB** | קריאת קבצים משיתוף ארגוני | מודיעין שמגיע כקבצים | **smbprotocol** |
| **AI Copilot** | השלמת חילוץ/העשרה/בעלות ב-JSON | טיפול בפערים בלי לשבור את הצינור | **httpx** |
| **SIEM / Sigma** | יצירת כללי ציד (Sigma, KQL, XQL וכו') | צוות הציד מקבל שאילתות מוכנות | תבניות Python מובנות |

---

## 7. מודלי נתונים עיקריים

### Vulnerability

**תיאור:** רשומת CVE הקנונית: מזהה, תיאור, CVSS/EPSS, יצרן/מוצר/גרסה, סטטוס צינור (`PipelineStatus`), דגלי שימוש ב-AI.

**למה זה נדרש:** זה האובייקט שכל המסכים והשלבים עובדים עליו.

**ספריות Python:** **SQLAlchemy**.

### PipelineStatus

**תיאור:** מצבים: `INGESTED`, `EXTRACTED`, `ENRICHED`, `MATCHED`, `ACTIONED`, `AI_FALLBACK`, `FAILED`.

**למה זה נדרש:** תצפית אחידה בממשק וב-API.

**ספריות Python:** `enum` + **SQLAlchemy**.

### AuditLog

**תיאור:** יומן לכל מעבר שלב: הודעה, שלב, JSON ב-`details`, חותמת זמן.

**למה זה נדרש:** לענות על "מה קרה ל-CVE הזה ולמה".

**ספריות Python:** **SQLAlchemy**.

### מקורות, אירועי קליטה, ריצות, נכסים, כרטיסים, זיהויים

**תיאור:** שומרים קונפיגורציית מקורות, payload גולמי, היסטוריית ריצה, התאמות נכסים, מפתחות Jira וארטיפקטי Sigma/SIEM.

**למה זה נדרש:** מעקב מקצה לקצה ומניעת כפילויות.

**ספריות Python:** **SQLAlchemy**.

---

## 8. עיבוד רקע

### תור עבודה בתוך התהליך

**תיאור:** אחרי שמירת האירוע ב-DB, ה-worker באותו תהליך מריץ את ה-orchestrator. אחרי ריסטארט אוסף אירועים שלא הסתיימו.

**למה זה נדרש:** לא לחסום את ה-HTTP בזמן קריאות ל-NVD/Jira, ולא לאבד עבודה בריסטארט.

**ספריות Python:** **asyncio**.

### תזמון (Scheduler)

**תיאור:** סורק במרווחים את פידי ה-API, Exchange, SMB, תיקייה מקומית ו-Web API.

**למה זה נדרש:** מקורות pull לא דוחפים לבד.

**ספריות Python:** **APScheduler**.

### Celery + Redis (אופציונלי)

**תיאור:** אותה עבודת pipeline על כמה מכונות.

**למה זה נדרש:** סקייל מעבר לשרת יחיד. לא חובה בהתקנת ברירת מחדל.

**ספריות Python:** **Celery**, **Redis**.

---

## 9. אמינות ותצפית

### מצב Dry-run

**תיאור:** בלי מפתחות Jira/Exchange/AI הצינור עדיין מסתיים ומדמה כרטיסים/מיילים.

**למה זה נדרש:** הדגמות ובדיקות בלי סודות.

**ספריות Python:** לוגיקת האפליקציה + **httpx** / **exchangelib** במצב לא מוגדר.

### ניסיונות חוזרים

**תיאור:** NVD/EPSS מנסים שוב אחרי כשל רשת זמני.

**למה זה נדרש:** APIs ציבוריים נופלים מדי פעם.

**ספריות Python:** **tenacity**.

---

## סיכום ספריות צד-שלישי (`requirements.txt`)

| חבילה | תפקיד |
|--------|--------|
| **FastAPI** | REST API ומסגרת האפליקציה |
| **Uvicorn** | שרת ASGI |
| **Starlette** | תשתיות HTTP (דרך FastAPI) |
| **Jinja2** | תבניות HTML |
| **python-multipart** | פרסור טפסים |
| **orjson** | JSON מהיר |
| **Pydantic** / **pydantic-settings** | ולידציה וקונפיגורציית `.env` |
| **SQLAlchemy 2** | ORM (SQLite / PostgreSQL) |
| **Alembic** | מיגרציות סכימה |
| **psycopg2-binary** | מנהל התקן PostgreSQL |
| **greenlet** | תמיכה ב-SQLAlchemy |
| **httpx** | לקוח HTTP ל-NVD, EPSS, Jira, CMDB, Sonatype, AI |
| **aiohttp** | HTTP אסינכרוני לפידים/עובדים |
| **APScheduler** | סריקה מתוזמנת של פידים ותיבות דואר |
| **exchangelib** | Microsoft Exchange / Outlook |
| **smbprotocol** | שיתוף SMB/CIFS (אישורי AD/LDAP) |
| **python-dateutil** | פרסור תאריכים |
| **tenacity** | ניסיונות חוזרים ל-NVD / EPSS |
| **structlog** | לוג מובנה |
| **Celery** + **Redis** | עובדים מבוזרים (אופציונלי) |
| **pytest** / **pytest-asyncio** | בדיקות |
