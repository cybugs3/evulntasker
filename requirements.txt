# EVulnTasker — production dependencies (Python 3.11+, RHEL 8+)
# Pin ranges that install cleanly from PyPI on RHEL 8 with python3.11.

# --- Web / API ---
fastapi>=0.115.0
uvicorn>=0.32.0
starlette>=0.41.0
jinja2>=3.1.4
python-multipart>=0.0.17
orjson>=3.10.0

# --- Settings & validation ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0

# --- Database ---
SQLAlchemy>=2.0.36,<2.1.0
alembic>=1.14.0
psycopg2-binary>=2.9.10; python_version < "3.14"
greenlet>=3.1.0

# --- HTTP clients ---
httpx>=0.27.0
aiohttp>=3.10.0

# --- Scheduling & background work ---
apscheduler>=3.10.4

# --- Email / Exchange ---
exchangelib>=5.4.0
aiosmtplib>=3.0.2

# --- SMB / CIFS (AD / LDAP credentials) ---
smbprotocol>=1.13.0

# --- Utilities ---
python-dateutil>=2.9.0
tenacity>=9.0.0
structlog>=24.4.0

# --- Optional: Celery worker (Redis broker). The default in-process
# asyncio queue does not require these packages. ---
celery[redis]>=5.4.0
redis>=5.1.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
