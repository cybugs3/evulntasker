# סקר סיכונים ובדיקת חדירה (PT) — EVulnTasker

| שדה | ערך |
| --- | --- |
| מערכת | EVulnTasker v1.0 Beta (Elizarov Vulnerabilities Tasking Manager) |
| סביבה שנבדקה | מעבדה ביתית / LAN — `http://192.168.1.104:8080` |
| תאריך | 17 בספטמבר 2026 |
| סוג בדיקה | סקר סיכונים White-box: סקירת קוד + אימות חי של משטחי HTTP **בלי** ניצול, בלי PoC, ובלי פעולות הרסניות |
| סיווג המסמך | פנימי — לסקירה משותפת לפני ייצור / חשיפה ארגונית |
| גרסת המסמך | 1.0 |

---

## 1. תקציר מנהלים

EVulnTasker היא פלטפורמת קליטת מודיעין CVE, העשרה, התאמה לקטלוג פנימי ופתיחת משימות (מייל / טיקטינג). במעבדה היא רצה כשירות systemd על פורט **8080 בכל הממשקים**, ב־**HTTP ללא TLS**, ו**ללא מסך התחברות, טוקן API, או תפקידים**.

המסקנה המרכזית: **כל מי שמגיע לפורט הוא מנהל המערכת**. זה לא באג נקודתי אלא מודל אבטחה של מעבדה. כל עוד זה כך, שאר הממצאים (קריאת הגדרות, מחיקת CVE, שינוי SMTP, קריאת תיקיות בשרת, שליחת מייל בשם הארגון) הם הרחבה של אותו פער — לא נדרש "לפרוץ" את האפליקציה.

באימות חי ב־17 בספטמבר 2026 התקבל `200` ללא אימות על:

- ממשק המפעיל (`/`) ו־Swagger (`/docs`, `/redoc`, `/openapi.json` — **90 נתיבים**)
- `GET /api/settings` (כולל מארח SMTP, שם משתמש Gmail, נתיב תיקייה מקומית)
- `GET /api/inventory` — **69** שורות קטלוג, מתוכן **68** עם `owner_email`
- `GET /api/sources`

Ticketing בדוא"ל **דולק**. סיסמת SMTP **שמורה**. נתיב הקליטה המקומית: `/tmp/evulntracker`. מסד הנתונים: SQLite.

**המלצת סיכון כוללת למעבדה:** מקובל כל עוד הפורט נשאר ברשת מבודדת, אין port-forward, ואין אמון בפרטיות של מיילים/סיסמאות שמוקלדות בדפדפן.  
**המלצה לייצור / רשת ארגונית:** לא לחשוף לפני reverse proxy עם TLS + SSO/Basic Auth + כיבוי `/docs`, ולוודא שפעולות הרס (Wipe / Reset / החלפת DB) דורשות תפקיד מנהל.

לא בוצעו התקפות, לא נשלחו בקשות שינוי למסד, ולא תועדו סודות במסמך זה.

---

## 2. היקף, מתודולוגיה ומגבלות

### 2.1 מה כן נכלל

- קוד האפליקציה (`app/`, תבניות, JS, systemd, `.env.example`)
- משטח ה־API כפי שמוגדר ב־OpenAPI החי
- אימות קריאה בלבד מול השרת החי `192.168.1.104:8080`
- מודל איום למפעיל, לרשת LAN, ולשרת שמריץ את השירות
- סיכוני שלמות (false match שמוביל לטיקט שגוי) כסיכון תפעולי

### 2.2 מה לא נכלל

- ניצול פעיל, fuzzing, brute-force, או סריקת פורטים
- בדיקת SMB/Exchange/CMDB/Sonatype/ITNM מול שרתים אמיתיים (לא מחוברים במעבדה)
- בדיקת החוסן של Gmail / NVD / EPSS עצמם
- סקירת הרשאות מערכת ההפעלה מעבר למה שמופיע ב־`systemd/evulntasker.service`
- בדיקת חדירה לרשת (VLAN, firewall, Wi‑Fi)

המסמך הוא **סקר סיכונים מבוסס קוד + תצפית**, לא דוח PT מלא לפי מתודולוגיית PTES/OSSTMM עם צוות אדום.

### 2.3 דירוג

| רמה | משמעות במסמך זה |
| --- | --- |
| Critical | שליטה מלאה או דליפת סודות/PII ללא תנאי, בסביבה החיה |
| High | יכולת לגרום נזק ממשי (מייל, קריאת קבצים, SSRF, שינוי תצורה) ברגע שיש גישה לפורט |
| Medium | מחמיר את המתקפה או חושף מידע עזר; או סיכון שמתעורר בהגדרה עתידית |
| Low | השפעה מוגבלת או מותנה בפיצ'ר כבוי |
| Info | תצפית / חוב טכני / סיכון שלמות נתונים |

---

## 3. מודל איום

```
[מפעיל בדפדפן] --HTTP--> [0.0.0.0:8080  EVulnTasker]
                              |-- SQLite / .env / logs על הדיסק
                              |-- SMTP Gmail (Act)
                              |-- NVD / EPSS / ATOM (יציאה לאינטרנט)
                              |-- תיקייה מקומית / SMB / Exchange (כניסת מודיעין)
                              `-- קטלוג פנימי (בעלים, מיילים, גרסאות)
```

| שחקן | יכולת רלוונטית |
| --- | --- |
| מפעיל לגיטימי במעבדה | כל הפעולות בממשק — זהו הייעוד |
| משתמש אחר ב־LAN | אותה יכולת, בלי סיסמה |
| אורח על Wi‑Fi / מכשיר נגוע ב־LAN | קריאה וכתיבה דרך HTTP, כולל יירוט סיסמאות אם מוקלדות |
| תוקף מרוחק | רק אם הפורט מנותב החוצה או שהשרת חשוף; לא נבדק |
| ספק LLM חיצוני | מקבל קטע קטלוג רק אם AI Enable דולק (כעת כבוי) |

נכסים רגישים: סיסמת Gmail App Password, קטלוג מערכות וכתובות בעלים, תוכן CVE גולמי, אפשרות לשלוח דוא"ל ארגוני, אפשרות למחוק את מסד ה־CVE.

---

## 4. טבלת ממצאים

| ID | חומרה | ממצא | סטטוס חי (17.09.2026) |
| --- | --- | --- | --- |
| PT-01 | Critical | אין אימות; האזנה על כל הממשקים ב־HTTP | מאומת |
| PT-02 | Critical | פעולות הרס ושינוי תצורה ללא תפקיד | קיים בקוד; לא הופעל בחי |
| PT-03 | High | PII והגדרות רגישות ב־JSON פתוח | מאומת (68 מיילים, SMTP user) |
| PT-04 | High | ערוץ Act כממסר דוא"ל בשליטת מי שמגיע לפורט | Ticketing Email דולק, סיסמה שמורה |
| PT-05 | High | נתיב תיקייה מקומית ללא allow-list | `/tmp/evulntracker`, Local דולק |
| PT-06 | High | SSRF / בדיקות חיבור לכתובת שהמפעיל קובע | ATOM / Test connection / Postgres test |
| PT-07 | Medium | תעבורת ניהול ללא TLS | `http://192.168.1.104:8080` |
| PT-08 | Medium | Swagger/ReDoc/OpenAPI חשופים | 90 נתיבים, `200` |
| PT-09 | Medium | אפשרות לכבות אימות תעודת TLS באינטגרציות | כרגע כבוי |
| PT-10 | Medium | סודות על דיסק (`.env`, SQLite) + מפתח ברירת מחדל | SQLite חי; `SECRET_KEY` דוגמה בקוד |
| PT-11 | Medium | `GET /api/sources` מחזיר `config` מלא ו־`webhook_token` | כרגע אין טוקן/סיסמה ב־config |
| PT-12 | Low | XML של ATOM בלי מנתח מחמיר | קיים בקוד |
| PT-13 | Low | שליחת קטלוג ל־LLM כש־AI דולק | AI כבוי, אין מפתח |
| PT-14 | Low | אין הגבלת קצב על API | קיים בקוד |
| PT-15 | Info | סיכוני שלמות התאמה / מייל (QA) | ראו סעיף 6 |

---

## 5. פירוט ממצאים

### PT-01 — אין שכבת זהות על משטח הניהול

**חומרה:** Critical  
**רכיבים:** `app/main.py`, `app.py`, `systemd/evulntasker.service`, היעדר `Depends` של Auth בכל נתיבי `/api/*`

השירות עולה עם Uvicorn על `0.0.0.0:8080`. אין cookie session, אין Bearer, אין Basic Auth, אין SSO. `EVULNTASKER_SECRET_KEY` קיים בקונפיג אבל **לא משמש לאימות**.

**אימות חי:** `GET /healthz`, `/`, `/api/settings`, `/api/inventory`, `/api/sources` — כולם `200` מדפדפן ללא התחברות.

**השפעה:** כל שאר הממצאים הקריטיים/גבוהים יורשים את זה. ברשת ארגונית זה שקול ללוח בקרה פתוח של SOC.

**תיקון מומלץ:**

1. Reverse proxy (nginx/Caddy) עם TLS ו־SSO או HTTP Basic לפחות.
2. אימות אפליקטיבי (משתמש/סיסמה או API token) + תפקידים: צופה / מפעיל / מנהל.
3. עד אז: firewall שמגביל את 8080 למארח המפעיל בלבד; לא לחשוף ל־WAN.

---

### PT-02 — פעולות הרס ושינוי תשתית ללא מחסום

**חומרה:** Critical  
**רכיבים:** `POST /api/settings/wipe-cve-data`, `POST /api/settings/reset-system`, `POST /api/settings/reset-internal-systems`, `PUT /api/settings/postgres`, `save_env` → כתיבה ל־`.env`

כל מי שמגיע לפורט יכול (ברמת הקוד):

- למחוק את מאגר ה־CVE שנאסף
- לאפס את הקטלוג הפנימי
- להחליף את `DATABASE_URL` ולכוון את התהליך ל־PostgreSQL אחר (כולל כתיבת הסיסמה ל־`.env`)
- לשמור מחדש סיסמאות SMTP/Jira/SMB דרך Settings

הבדיקה **לא** שלחה את הקריאות האלה.

**השפעה:** אובדן מודיעין, אובדן מלאי, או חטיפת מסד אם התוקף מצביע על שרת בשליטתו.

**תיקון מומלץ:** תפקיד מנהל + אישור כפול (typed confirm) לפעולות Wipe/Reset; הגבלת שינוי `DATABASE_URL` לקובץ מקומי/root בלבד; ביקורת (audit log) עם משתמש אמיתי.

---

### PT-03 — חשיפת PII והגדרות רגישות ב־API

**חומרה:** High  
**רכיבים:** `GET /api/settings`, `GET /api/inventory`, `GET /api/inventory` export CSV, מסכי Incoming / Workflow / Debugger

`GET /api/settings` מסתיר סיסמאות (`password_set`) — זה נכון — אבל חושף:

- מארח ומשתמש SMTP, כתובת From
- נתיב תיקייה מקומית
- שרת SMB, מארחי CMDB/Jira אם הוגדרו
- דגל האם מפתח NVD/AI שמור

`GET /api/inventory` מחזיר את הקטלוג המלא כולל `owner_email`. בחי: **68/69** שורות עם מייל.

**השפעה:** מיפוי אנשי קשר ומערכות הארגון, הכנה לפישינג ממוקד, והבנת טופולוגיית האינטגרציות — בלי לנחש סיסמאות.

**תיקון מומלץ:** אחרי הוספת login — הרשאת "צופה" בלי מיילים מלאים; מסך Settings רק למנהל; ייצוא CSV מסומן כנתון רגיש.

---

### PT-04 — צינור Act כממסר דוא"ל

**חומרה:** High  
**רכיבים:** `app/pipeline/step5_act.py`, Ticketing Email, Gmail App Password ב־`.env`

בחי: `ticketing.enables.email = true`, `smtp_host = smtp.gmail.com`, סיסמת SMTP שמורה.

מי ששולט בממשק יכול:

- לשנות נמעני Hunt / fallback
- להוסיף שורת קטלוג עם מייל יעד
- להריץ Re-run על CVE שכבר הותאם, כך שהשלב Act ישלח דוא"ל מחשבון Gmail ששמור במערכת

Debugger עצמו **לא** שולח (persist כבוי בקוד) — זו בקרה חיובית. הצינור החי כן שולח.

**השפעה:** שימוש במוניטין של תיבת הארגון לשליחת הודעות; דליפת תוכן CVE+התאמות לנמען שקבע התוקף.

**תיקון מומלץ:** Act רק אחרי login; allow-list של דומיין נמענים; rate limit על שליחה; אישור ידני ראשון לסביבת ייצור.

---

### PT-05 — תיקיית Local ללא רשימת היתר

**חומרה:** High  
**רכיבים:** `app/services/file_ingest.py` (`local_folder`), `POST /api/settings/local`, `POST /api/settings/local/test`, סנכרון Local

הנתיב שהמפעיל מזין עובר `Path.resolve` ובדיקת קיום/קריאה בלבד. אין כליאה תחת inbox ייעודי (למשל `/var/evulntasker/inbox`). Test מחזיר שמות קבצים; הסנכרון קורא קבצי טקסט עד 20MB.

בחי: Local דולק, הנתיב `/tmp/evulntracker`.

**השפעה:** ברגע שיש גישה ל־API, אפשר לכוון את הקליטה לתיקייה קריאה אחרת על השרת (קובצי תצורה, לוגים, שיתופים מורשים למשתמש `evulntasker`) ולשאוב תוכן טקסטואלי לתוך הצינור.

**תיקון מומלץ:** allow-list של שורש אחד; דחיית נתיבים מחוץ לו; הרצת השירות עם user מוגבל בלי גישה ל־home של אחרים; לא להריץ כ־root (היחידה מוגדרת `User=evulntasker` — לשמור על זה).

---

### PT-06 — בקשות יוצאות לכתובת שהמשתמש קובע (SSRF)

**חומרה:** High  
**רכיבים:** ATOM `httpx.get(url)`, בדיקות CMDB/Sonatype/ITNM/Ticketing (`probe_connection`), `POST /api/settings/postgres/test`

אין רשימת היתר למארחים. השרת יוצא מהמארח שלו אל כל URL/host שמוזן ב־Settings. זה מתאים למפעיל לגיטימי, ומסוכן כשאין אימות.

**השפעה:** סריקה פנימית דרך השרת (localhost, שירותי ניהול, metadata אם קיים בענן), או גרימה לשרת להתחבר למאגר חיצוני.

**תיקון מומלץ:** אחרי auth — עדיין כדאי לחסום כתובות פרטיות/loopback ב־Test אם לא במצב lab; timeout וגודל תגובה כבר קיימים חלקית.

---

### PT-07 — ניהול ב־HTTP ללא TLS

**חומרה:** Medium  
**רכיב:** האזנה ישירה של Uvicorn, בלי redirect ל־HTTPS

סיסמת Gmail, טוקני Jira, וסיסמאות SMB/Exchange מוקלדות בטפסי Settings ועוברות ב־JSON על ה־LAN בבהירות.

**תיקון מומלץ:** TLS ב־proxy בלבד; לא לחשוף 8080 מחוץ ל־localhost על השרת.

---

### PT-08 — תיעוד OpenAPI פתוח

**חומרה:** Medium  
**אימות חי:** `/docs`, `/redoc`, `/openapi.json` — `200`, 90 נתיבים כולל wipe/debug/settings.

FastAPI ברירת מחדל (`docs_url` לא כובה ב־`app/main.py`).

**תיקון מומלץ:** `docs_url=None`, `redoc_url=None`, `openapi_url=None` כש־`EVULNTASKER_ENV=production`, או להשאיר רק אחרי login.

---

### PT-09 — Ignore TLS certificate

**חומרה:** Medium (מותנה)  
**רכיבים:** `smtp_relay_ignore_cert`, `CMDB_IGNORE_CERT`, `TICKETING_IGNORE_CERT`, וכו'

בחי כל הדגלים **כבויים**. אם יופעלו, סיסמאות לאינטגרציות יישלחו גם מול שרת מתחזה.

**תיקון מומלץ:** להשאיר כבוי בייצור; אם חייבים מעבדה — לוג אזהרה בולט (כבר יש UI checkbox).

---

### PT-10 — סודות על דיסק ומפתח ברירת מחדל

**חומרה:** Medium  
**רכיבים:** `.env` (נכתב מ־Settings), SQLite תחת `data/`, `logs/`, `secret_key` default `change-me-in-production`

`.gitignore` ממדר `.env` / `data/` / `logs/` מגיט — נכון. אם שיתוף SMB מצביע על עץ ההרצה, יש לוודא ש־`.env` ו־`*.db` **לא** נגישים לכל מי שרואה את השיתוף.

ברירת המחדל בקוד ל־SQLite היא `./data/evulntasker.db`; במעבדה ייתכן שעדיין רץ קובץ ישן (`vulnintel.db`) — זה חוב תפעולי, לא חולשת ניצול.

**תיקון מומלץ:** הרשאות `0600` על `.env` ועל קובץ ה־DB; `SECRET_KEY` אקראי גם אם עדיין אין סשנים; לא לשתף את תיקיית runtime ב־SMB.

---

### PT-11 — סכמת `SourceOut` דולפת config גולמי

**חומרה:** Medium (כעת latent)  
**רכיב:** `GET /api/sources` → `SourceOut.config` + `webhook_token`

`GET /api/settings` מסנן `password`/`token`. `GET /api/sources` מחזיר את מודל ה־ORM כמו שהוא. בחי: אין `webhook_token` ואין סיסמה ב־config הנוכחי. ברגע שיופעל Webhook או יישמר token ב־config, ה־GET הזה יחשוף אותו לכל קורא.

**תיקון מומלץ:** אותה פונקציית `public_source` גם כאן; לא להחזיר `webhook_token` בגוף הרשימה (רק פעם אחת למנהל בזמן יצירה).

---

### PT-12 — ניתוח XML של פידים

**חומרה:** Low  
**רכיב:** `xml.etree.ElementTree.fromstring` ב־`app/services/atom_feed.py`

CPython לא פותר ישויות חיצוניות כברירת מחדל, לכן XXE קלאסי פחות סביר. פיד ענק / מקונן עלול להעמיס CPU/זיכרון בתהליך היחיד (HTTP + worker + scheduler באותו תהליך).

**תיקון מומלץ:** הגבלת גודל גוף (כבר יש timeouts); שקול `defusedxml` אם פידים מגיעים ממקור לא מהימן.

---

### PT-13 — דליפת מלאי לספק AI

**חומרה:** Low (כעת כבוי)  
**רכיב:** `app/integrations/ai_copilot.py` — נשלחים עד 25 פריטי קטלוג (שם/ספק/מוצר/גרסה, בלי מייל) כש־enrichment עם AI דולק.

בחי: `ai.enabled=false`, `api_key_set=false`.

**תיקון מומלץ:** להשאיר כבוי עד שיש מדיניות ספק; לא לשלוח שמות שרתים פנימיים אם אפשר.

---

### PT-14 — אין הגבלת קצב

**חומרה:** Low  
**רכיבים:** `/api/debug/run`, ingest, Re-run, קריאות NVD

תהליך אחד. הצפה תמימה או זדונית תתקע העשרה / תשלח מיילים / תמצה מכסת NVD.

**תיקון מומלץ:** אחרי auth — מגבלה פשוטה לפי IP/משתמש על debug ו־wipe.

---

### PT-15 — סיכוני שלמות (לא חדירה)

מתוך QA תפעולי בסביבה החיה, רלוונטיים ל"סיכון" גם בלי תוקף חיצוני:

| נושא | סיכון | הערה |
| --- | --- | --- |
| התאמת `linux-os-family` רחבה מדי | טיקטים לבעלי Windows/glibc על CVE ליבה | תוקן בקוד; דורש restart + Re-run |
| תווי Bidi במיילי קטלוג | כשל SMTP אחרי שחלק מהנמענים כבר קיבלו | תוקן בקוד (`clean_email`) |
| From מול Gmail | המעטפה נשארת שם המשתמש | מגבלת Gmail, לא חולשת PT |
| אין login | ראו PT-01 | פער מוצר ידוע ב־`GAPS.md` |

אלה סיכוני **אמון בהתראות**, לא וקטור חדירה לשרת.

---

## 6. בקרות חיוביות שנצפו

המערכת לא "פרוצה מבפנים" במובן הקלאסי של הזרקות:

- שאילתות דרך SQLAlchemy ORM; מחיקות Wipe משתמשות בשמות טבלאות מהמטא־דאטה, לא מקלט משתמש.
- סיסמאות מוסרות מ־`GET /api/settings` (`password_set` בלבד).
- Debugger מאלץ `persist=False` — לא שולח מייל ולא פותח טיקט.
- טוקן webhook נוצר ב־`secrets.token_urlsafe(24)` (אם יופעל מקור webhook).
- תבניות מייל HTML עם Jinja2 autoescape; ב־JS של הממשק לא נמצא `innerHTML`.
- קליטת קבצים מדלגת על בינארי (`NUL`) ומגבילה ל־20MB.
- יחידת systemd רצה כמשתמש `evulntasker`, לא כ־root (לפי קובץ השירות שבעץ).
- דגלי `ignore_cert` במעבדה כבויים.
- AI enrichment כבוי.

---

## 7. מפת טיפול מומלצת

### לפני חשיפה מעבר למעבדה (חובה)

1. סגירת 8080 בחומת אש למארח המפעיל / localhost + proxy.
2. TLS ב־reverse proxy.
3. אימות (לפחות Basic ב־proxy; עדיף SSO).
4. כיבוי `/docs` `/redoc` `/openapi.json` בפרודקשן.
5. הרשאות קובץ על `.env` ועל SQLite; לא לשתף אותם ב־SMB.

### אחרי זה (שיפור מוצר)

6. תפקידים: צופה / מפעיל / מנהל; Wipe/Reset/Postgres רק למנהל.
7. סינון `GET /api/sources` כמו Settings.
8. Allow-list לנתיב Local.
9. Allow-list דומיין לנמעני Act.
10. Audit log עם זהות משתמש.

### לא חוסם מעבדה, אבל כדאי

11. Restart אחרי תיקוני ה־QA (התאמת Linux, ניקוי מיילים) ואז Re-run ל־CVE שנכשלו ב־Act.
12. Alembic אמיתי לפני מעבר ל־PostgreSQL (פער ידוע, לא PT).

---

## 8. הערכת סיכון שיורי

| תרחיש | סיכון נוכחי | אחרי צעדי "חובה" בסעיף 7 |
| --- | --- | --- |
| גורם ב־LAN נכנס לממשק כמנהל | גבוה מאוד | נמוך |
| דליפת קטלוג + מיילי בעלים | גבוה | נמוך (מאומת) |
| שליחת מייל בשם Gmail של המעבדה | גבוה (Email דולק) | נמוך |
| קריאת תיקיות על השרת דרך Local | בינוני־גבוה | נמוך (allow-list + user מוגבל) |
| MITM על סיסמת SMTP | בינוני ב־LAN | נמוך (TLS) |
| ניצול SQLi/RCE באפליקציה | לא זוהה בסקירה זו | ללא שינוי מהותי |

**פסק דין:** מתאים להמשך עבודה במעבדה מבודדת. **לא מתאים** לחיבור לרשת ארגונית, ל־VPN רחב, או לפורט פתוח — עד שיושמים לפחות את סעיפי החובה.

---

## 9. נספח — משטח API רגיש (שמות בלבד)

מתוך OpenAPI החי, בין היתר:

- `POST /api/settings/wipe-cve-data`
- `POST /api/settings/reset-system`
- `POST /api/settings/reset-internal-systems`
- `PUT /api/settings/postgres`
- `POST /api/settings/postgres/test`
- `POST /api/settings/local` + `/local/test`
- `POST /api/settings/ticketing`
- `POST /api/debug/run` ו־`/api/debug/run/stream`
- `POST /api/webhooks/{token}`
- `GET /api/settings`
- `GET /api/inventory`

אין במסמך גופי בקשה, מטענים, או הוראות הפעלה.

---

## 10. חתימת הבדיקה

| | |
| --- | --- |
| שיטה | סקירת קוד + GET חי לאימות חשיפה |
| מה לא נעשה | POST הרסני, ניצול, סריקת רשת, פיצוח סיסמאות |
| תלות | הממצאים מניחים שהשירות החי תואם לעץ הקוד שנבדק; חלק מתיקוני QA טרם עברו restart |
| המשך מומלץ | מעבר משותף על סעיף 7, ואז PT ממוקד אחרי שיתווסף login |

סוף המסמך.
